<?php

return [

    /*
    |--------------------------------------------------------------------------
    | نطاقات فرعية محجوزة (لا تُطابق فرعاً بالـ slug)
    |--------------------------------------------------------------------------
    */
    'reserved_subdomains' => [
        'www',
        'admin',
        'api',
        'app',
        'mail',
        'ftp',
        'cdn',
        'dashboard',
        'storage',
        'livewire',
    ],

    /*
    |--------------------------------------------------------------------------
    | كاش مطابقة المضيف → فرع (ثوانٍ)
    |--------------------------------------------------------------------------
    | 0 = تعطيل. قيمة مثل 3600 تقلل استعلامات DB على كل طلب عام.
    | يُبطَل الكاش تلقائياً عند حفظ/حذف سجل فرع (انظر BranchResolver).
    */
    'resolve_cache_ttl' => (int) env('BRANCHES_RESOLVE_CACHE_TTL', 3600),
];
